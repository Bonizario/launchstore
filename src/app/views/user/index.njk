{% import 'parts/delete.njk' as userConfirm %}
{% import 'parts/card.njk' as card %}

{% extends 'layout.njk' %}
{% block content %}
{% call card.render('Votre compte', 'small container ') %}
<form method="POST" action="/users?_method=PUT">
  <div class="fields">
    {% include "user/fields.njk" %}
    <input
      type="hidden"
      name="id"
      value="{{ user.id }}"
    >
  </div>
  <button class="button edit" type="submit">Mettre à jour</button>
</form>
<form class="form-delete" action="/users?_method=DELETE" method="POST">
  <input class="inv" type="hidden" name="id" value="{{ user.id }}">
  <button class="button delete" type="submit">Supprimer</button>
</form>
{% endcall %}
{{ userConfirm.delete("'Voulez-vous vraiment supprimer votre compte ?'") }}
{% endblock %}